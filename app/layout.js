import './globals.css';

export const metadata = {
  title: 'dummy-nextjs-prisma-nomig',
  description: 'DEParture Next.js deployment sample',
};

export default function RootLayout({ children }) {
  return <html lang="ja"><body>{children}</body></html>;
}
